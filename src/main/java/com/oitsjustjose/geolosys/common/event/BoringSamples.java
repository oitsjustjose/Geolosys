package com.oitsjustjose.geolosys.common.event;

public class BoringSamples {
    /**
     * This event is only registered via {@link com.oitsjustjose.geolosys.Geolosys}
     * Assuming that it's only registered assuming config enables it
     */
    // public void registerEvent(BlockEvent.HarvestDropsEvent event)
    // {
    // if (event.getState().getBlock() instanceof SampleBlock)
    // {
    // PlayerEntity player = event.getHarvester();

    // event.getDrops().clear();
    // Geolosys.proxy.sendProspectingMessage(player,
    // Utils.blockStateToStack(event.getState()), null);
    // }
    // }
}